f=open('notes.txt', 'r')
llista=[]
dicc={}
for linian in f:
    linia=linian.strip().split()
    llista.append(linia)
del[llista[0]]
for i in llista:
    dicc[i[1]]=i[0]

print(dicc)
print(list(dicc.keys())[0])
